// Program to demonstrate length() method in String
// length() returns ->  number of characters in the string

package strings.methodsinstring;

public class method2length {
    public static void main(String[] args) {
        String name = "akshat";
        int len = name.length();
        System.out.println("Length of " + name + " is: " + len);
    }
}
